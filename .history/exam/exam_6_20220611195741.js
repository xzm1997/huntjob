const read_line = require('readline-sync')
let num = read_line.question(""), input = []
num = parseInt(num);
console.log(num);

const judge = (arr) => {
  if
}

for (let i = 0; i < num; ++i) {
  let arrayNum = read_line.question("");
  input.push(read_line.question("").split(' ').map(item => parseInt(item)))

}

console.log(input);
