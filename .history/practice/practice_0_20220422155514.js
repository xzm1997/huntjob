let input = 'ATTTAA'.split('');
let res = input, stack = [[], []];

res.forEach((item, index) => {
  if (item === 'A') this.stack[0].push[index];
  else this.stack[1].push[index];
})

console.log(stack);